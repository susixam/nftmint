{
  "name": "nft-mint",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "forge script script/Deploy.s.sol --target-contract DeployScript --rpc-url sepolia --broadcast --verify --delay 15",
    "deploy:dry": "forge script script/Deploy.s.sol --target-contract DeployScript --rpc-url sepolia",
    "deploy:node": "forge build && node scripts/deploy-node.js"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.6",
    "dotenv": "^16.4.5",
    "ethers": "^6.13.0"
  }
}
